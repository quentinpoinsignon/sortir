{% extends 'base.html.twig' %}

{% block title %} | Créer une sortie {% endblock %}

{% block body %}
    <br><br>
    <h1>Créer une sortie !</h1>
    {{ form_start(eventForm) }}
    {{ form_widget(eventForm) }}
    <br>

    <button class="w3-btn w3-white w3-round" type="submit">
        Connexion
    </button>


    {{ form_end(eventForm) }}
{% endblock %}