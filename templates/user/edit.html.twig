{% extends 'layout.html.twig' %}

{% block title %} | Modifier profil{% endblock %}

{% block body %}
    <div class="w3-panel">
    <h1 style="margin-top: 50px;">Modifier profil</h1>
        <div class="formulaire" style="width: 70%;">
            <div class="btn">
                {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
        </div>
{#    <a href="{{ path('user_index') }}" class="w3-btn w3-teal w3-round" >back to list</a>#}

    {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
