{% extends 'base.html.twig' %}

{% block title %}Consulter un profil{% endblock %}

{% block body %}
    <br>
    <div class="w3-card w3-margin w3-center w3-padding">
        <h1>{{ user.username }}</h1>
        <p>Prénom : {{ user.firstName }}</p>
        <p>Nom : {{ user.name }}</p>
        <p>Téléphone : {{ user.phoneNumber }}</p>
        <p>Email : {{ user.emailAdress }}</p>
        <p>Campus : {{ user.campus.name }}</p>
{#        <a href="{{ url('event_detail', {'id':event.id, 'registrations':registrations, 'event':event}) }}" class="w3-btn w3-teal w3-round">Retour</a>#}
        <button class="w3-btn w3-teal w3-round" onclick="goBack()">Retour</button>
    </div>


    <script>
        function goBack() {
            window.history.back();
        }
    </script>
{% endblock %}
